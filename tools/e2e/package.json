{
  "name": "ht-resourcemapper-e2e",
  "private": true,
  "description": "Playwright-driven browser verification helpers and committed regression suite for the HT.ResourceMapper Blazor Server UI (uses system Chrome, no bundled Chromium download).",
  "version": "1.0.0",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "playwright-core": "^1.48.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
